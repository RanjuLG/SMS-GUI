export interface User {
    id:string;
    userName?: string;
    email?: string;
    roles?: string[];
    //selected?: boolean;
  }

  export interface UserDTO {
  
    userName?: string;
    email?: string;
    roles?: string[];
    //selected?: boolean;
  }
  